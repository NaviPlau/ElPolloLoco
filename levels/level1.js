let level1;
/**
 * Initializes the game level by creating a new instance of the `Level` class.
 * The level is set up with empty arrays for enemies, clouds, background objects, bottles, coins, and other elements.
 * This function sets up `level1` as the current level.
 */
function initLevel() {
  level1 = new Level(
    [],
    [],  
    [],  
    [],  
    [],  
    []   
  );
}